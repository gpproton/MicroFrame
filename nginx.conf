##############################
##
## Config initialization..
##
##############################

autoindex off;

autoindex off;

location / {
  if (!-e $request_filename){
    rewrite ^(.*)$ /index.php?controller=$1 break;
  }
  rewrite ^(.+)$ /index.php?controller=$1 break;
}